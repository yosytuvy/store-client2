
export const boxStyles = {
    display: "flex",
    flexWrap: "wrap",
    "& > :not(style)": {
      m: 2,
      width: 200, 
      height: 200,
      borderRadius: 4
    }
  }

export const innerBoxStyles = {
    height: "80%",
    width: 200,
    borderRadius: 4,
  }
